table:
  schema: mb_views
  name: access_key_activity
select_permissions:
  - role: anonymous
    permission:
      columns:
        - timestamp
        - receipt_id
        - account_id
        - public_key
        - kind
      filter: {}
      allow_aggregations: true
