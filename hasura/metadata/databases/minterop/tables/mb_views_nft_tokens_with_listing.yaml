table:
  schema: mb_views
  name: nft_tokens_with_listing
select_permissions:
  - role: anonymous
    permission:
      columns:
        - nft_contract_id
        - token_id
        - owner
        - metadata_id
        - reference_blob
        - price
        - content_flag
      filter: {}
      allow_aggregations: true
