table:
  schema: mb_views
  name: nft_tokens_with_listings
select_permissions:
  - role: anonymous
    permission:
      columns:
        - nft_contract_id
        - token_id
        - owner
        - metadata_id
        - price
      filter: {}
      allow_aggregations: true
