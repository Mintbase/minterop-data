table:
  schema: public
  name: access_keys
select_permissions:
  - role: anonymous
    permission:
      columns:
        - account_id
        - public_key
        - created_at
        - removed_at
      filter: {}
      allow_aggregations: true
