table:
  schema: public
  name: access_keys
select_permissions:
  - role: anonymous
    permission:
      columns:
        - account_id
        - public_key
        - created_at
        - created_receipt_id
        - removed_at
        - removed_receipt_id
      filter: {}
      allow_aggregations: true
