table:
  schema: analytics_tmp
  name: referrer_earnings
select_permissions:
  - role: anonymous
    permission:
      columns:
        - date_trunc
        - referrer_id
        - sales_count
        - total_earned
      filter: {}
      allow_aggregations: true
