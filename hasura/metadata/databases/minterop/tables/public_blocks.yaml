table:
  schema: public
  name: blocks
select_permissions:
  - role: anonymous
    permission:
      columns:
        - synced_height
        - synced_height_tmp
      filter: {}
      allow_aggregations: true
